services:
  frontend:
    build:
      context: ./Frontend/pr_scoring_enginge
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    restart: unless-stopped

  backend:
    build:
      context: ./Backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    restart: unless-stopped
